/**
 * Lightweight stub tools module to satisfy build while preserving runtime agent behavior.
 * Replace with full implementations as needed.
 */

import 'dotenv/config';

// Lightweight stub tools module to satisfy build while preserving runtime agent behavior.
// Replace with full implementations as needed.

export const allTools: any[] = [];

export default allTools;
